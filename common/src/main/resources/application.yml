# dev
'%dev':
  quarkus:
    datasource:
      db-kind: postgresql
      jdbc:
        max-size: 20
        extended-leak-report: true
        url: jdbc:postgresql://bj-pg.vaedzy.com:29874/hispread_gpt
      username: root
      password: ZhiforCloud0719.
    hibernate-orm:
      database:
        generation: none
      datasource: <default>
      packages: com.yujianyou.entity
    mybatis:
      map-underscore-to-camel-case: true
      use-generated-keys: true
      mapper-locations: mapper
      # 开启Mybatis日志打印
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
